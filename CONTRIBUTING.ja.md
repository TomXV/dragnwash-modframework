# コントリビューション

[English](CONTRIBUTING.md)

見に来てくれてありがとうございます。これは前提 Mod で、ほかの人の Mod がこの上に
立ちます。ですから変更の基準は「5 つの Mod が同時に使っても成り立つか」で、小さくて
地味なプルリクエストがたいてい一番ありがたいものです。

参加するみなさんには[行動規範](docs/CODE_OF_CONDUCT.ja.md)に沿ってもらいます。
セキュリティの問題はプルリクエストや公開の Issue ではなく、
[SECURITY.ja.md](SECURITY.ja.md) の窓口へお願いします。

## コードを書かない手伝い

- **壊れたことを報告する。** `BepInEx/LogOutput.log`、ゲームのビルド、環境が入った
  不具合の報告はとても価値があります。とくにゲームのアップデートの直後は。
  - **ラベル：** 新しい Issue には自動でラベル（種類・対象・深刻度）が付きます。
    バージョン・手順・ログが足りない不具合報告には、それをお願いするコメントが
    1 回だけ付きます。
  - **送るもの：** そのために、タイトルと本文の文章（コードブロック・表・画像は
    除く）と、貼られたログのエラー行（パスのユーザー名は伏せます）を TypeSafe AI の
    分類モデルに送っています（[`tools/issue-triage.py`](tools/issue-triage.py)）。
  - **Issue はすべて人が読みます。**
- **自分の Mod にできないことを言う。** このフレームワークは、Mod が同じ機構を何度も
  作り直していたから生まれました。フレームワークに手段がないせいでゲームを直接
  パッチしているなら、それがいちばん役に立つ Issue です。
- **ドキュメントを直す。** プレイヤー向け・Mod 作者向けの説明は
  [Wiki](https://github.com/TomXV/dragnwash-modframework/wiki/Home-ja) にあります。
  Wiki は別のリポジトリで、GitHub の Wiki はプルリクエストを受け取れないので、
  どのページのどこが違うのかを Issue に書いてください。設計と調査の記録は
  こちらの [`docs/`](docs/) に残ります。
- **変わった環境で試す。** Steam Deck、SteamOS 以外の Linux、ARM 版 Windows。
  動いても動かなくても結果は役に立ちます。よければ
  [`docs/GAME_BUILDS.md`](docs/GAME_BUILDS.md) に書き足してください。
- **スポンサーになる。** そうしたくて、できるならば。必須ではありませんし、その先に
  隠してあるものもありません：https://github.com/sponsors/TomXV

## 大きなものに手を付ける前に

まず Issue を立ててください。

- **Issue が要るもの：** 新しいライブラリ、公開 API の変更、Mod の読み込み方に
  関わる変更。書く前に合意しておく価値があります。
- **理由：** 作業が無駄になるのを避けるためもありますが、
  [`docs/DESIGN.ja.md`](docs/DESIGN.ja.md) と [`docs/ROADMAP.ja.md`](docs/ROADMAP.ja.md)
  に、あなたの考えとは違う筋書きがすでにあるかもしれないからです。

小さな修正にその手続きは要りません。そのまま送ってください。

## 準備

1. .NET SDK を入れ、ゲームに BepInEx 5.4.23.5 を入れます。
2. 自分のゲームのインストール先から参照アセンブリをコピーします。

   ```bash
   pwsh tools/copy-libs.ps1
   ```

   ゲームが既定の Steam ライブラリにないときは `-GamePath` を渡してください。
   コピー先は `libs/` フォルダーで、これは無視される場所です。
   **絶対にコミットしないでください。**
3. 作業するものをビルドします。

   ```bash
   dotnet build src/DragNWash.ModFramework/DragNWash.ModFramework.csproj -c Release
   ```

4. 各プラグインの DLL を `<Game>/BepInEx/plugins/<アセンブリ名>/` に、
   `DragNWash.ModFramework.Preloader.dll` を `<Game>/BepInEx/patchers/` に置きます。

同じ手順の詳しい版は [README.ja.md](README.ja.md) に、ライブラリごとの役割は
[Playing well with others (wiki)](https://github.com/TomXV/dragnwash-modframework/wiki/Playing-well-with-others-ja)
にあります。

## 守ってほしい決まり

好みの問題ではありません。これを破ったプルリクエストはマージできません。

- **ゲームのファイル、BepInEx のバイナリ、`libs/` の中身を絶対にコミットしない。**
  すべての push とプルリクエストでチェックが走り、引っかかればビルドは失敗します。
  このリポジトリを公開していられるのは、この決まりのおかげです。
- **ゲーム由来の素材は [`docs/CONTENT_POLICY.ja.md`](docs/CONTENT_POLICY.ja.md) に
  従う。** 手で作ったもの、別のものに作り変えたものは構いません。ゲームのデータ
  そのままは駄目です。
- **ゲームのクラスに触れるコードは `internal` のまま。** Mod から見えるのは
  フレームワーク自身の型だけです。この境界こそが要点で、ゲームが更新されたときに
  追従が必要なのがフレームワークだけで済む理由です。
- **公開 API の互換性を壊す変更にはメジャーバージョンの更新と、相応の理由が要る。**
  その上に立つ Mod も一緒に壊れるからです。

## チェック

CI はゲームのファイルを必要としないものを走らせます。手元でも全部走らせられます。

```bash
python tools/check-repo.py        # バージョン、GUID、CHANGELOG、ドキュメントのリンク
python tools/linekeys.py --check  # 行キーがベクターと一致しているか
python tools/check-commits.py     # コミットメッセージに道具の署名が入っていないか
```

Docker があれば、この全部と、CI がやるビルドまで、CI と同じイメージの中で回せます。手元に Python も .NET SDK も要りません。

```bash
docker compose run --rm checks
```

詳しくは [docs/DOCKER.ja.md](docs/DOCKER.ja.md) を見てください。

最後のものが**コミットチェッカー**です。この履歴に名前が載るのは、そのコミットが
何を言うべきかを決めた人だけで、それを打ち込んだエディターや補助ツールや IDE では
ありません。次の 2 つでビルドが失敗します。

- **メッセージ** — 道具を挙げた `Co-authored-by` の行、"Generated with" の
  フッター、補助ツールのセッションへのリンク。人間の共著者は歓迎です。
- **作者またはコミッター** — メッセージがどれだけ綺麗でも、コミットの署名が道具の
  アカウントなら弾きます。`dependabot[bot]`、`github-actions[bot]`、Web でマージ
  したときの `GitHub <noreply@github.com>` は通ります。人の名前も通ります。
  Claude は実在する人名なので、モデル名や `[bot]` が後ろに付いて初めて弾きます。

引っかかったらコミットを直して（直前なら `git commit --amend`、作者が違うなら
`--reset-author` を足す、古いものなら `git rebase -i`）、もう一度 push して
ください。

中核とライブラリはゲームのアセンブリが要るため、CI ではビルドできません。つまり、
それを確かめたのは**あなた**です。どのゲームのビルドの、どの環境で、何を見て
確かめたのかをプルリクエストに書いてください。

## バージョンと CHANGELOG

プロジェクトごとにバージョンがあります。変えるときは：

- `.csproj` の `<Version>`、コード中の `Version` 定数、[CHANGELOG.md](CHANGELOG.md)
  の `### <名前> <バージョン>` の見出し。この 3 つを合わせてください。
  `tools/check-repo.py` が見ているのはまさにここです。
- プリローダーのパッチャーは中核のバージョンに追従します。

たいていのプルリクエストではバージョンを上げません。ふつうはリリースをまとめる
ときの作業です。

## ドキュメント

`docs/` のものはすべて 2 か国語（`NAME.md` と `NAME.ja.md`）で、互いにリンクして
います。できれば両方直してください。片方しかできないときは、その旨をプルリクエスト
に書いてくれれば引き取ります。訳がないことを理由に修正を止めはしません。

Wiki は別のリポジトリで、プルリクエストは受け取れません。変更によって Wiki の
どこかが古くなるなら、どのページかを書いてください。

## プルリクエストそのもの

- `main` から分岐し、1 つのプルリクエストに 1 つの話題を。
- 1 年後に履歴を読む人が、何がなぜ変わったのか分かるように書いてください。
  テンプレートがその質問をします。
- 本文は日本語でも英語でも構いません。両方書く必要はありません。
- 書きかけ（Draft）のプルリクエストも歓迎です。完成させる前に設計を相談したい
  ときにもどうぞ。

プルリクエストを送ることで、その内容がほかのコードと同じく
[MIT ライセンス](LICENSE)で提供されることに同意したものとします。

## 最後にひとつ

これは非公式のファンプロジェクトで、Gator Dragon Games とは関係ありません。
Mod を入れているときだけ起きる不具合をゲームの開発元に報告したり、ここにあるものの
サポートを開発元に求めたりしないでください。
