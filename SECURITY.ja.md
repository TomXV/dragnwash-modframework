# セキュリティポリシー

[English](SECURITY.md)

Drag'n Wash ModFramework はゲームの非公式 Mod で、サービスではありません。それでも
ほかの人のパソコンでコードを動かしたり、ファイルを置いたりはするので、そこについての
報告は歓迎しますし、ちゃんと受け止めます。

## 脆弱性の報告

**セキュリティの問題は、公開の Issue には書かないでください。**

代わりに GitHub の非公開の報告を使ってください。
[**Report a vulnerability**](https://github.com/TomXV/dragnwash-modframework/security/advisories/new)
（このリポジトリの **Security** タブ → **Report a vulnerability**）から送れます。
報告はあなたとメンテナーにしか見えず、修正が出るまで非公開のままです。

書いてもらえると助かるのは、次のようなことです。

- 攻撃者に何ができて、そのために何が要るのか（プレイヤーがダウンロードする
  ファイル？　すでに入っている Mod？　攻撃者が用意したネットワーク？）
- 再現の手順と、そのときのフレームワーク・ゲーム・BepInEx のバージョン
- ログがあればそれも。`BepInEx/LogOutput.log` か、[クラッシュレポート](https://github.com/TomXV/dragnwash-modframework/wiki/Crash-reports-ja)
  が保存したファイルです

一人でやっている趣味のプロジェクトなので、最初の返事は 1 週間以内を目安にして
います。1 週間たっても何もなければ、同じアドバイザリのスレッドで声をかけて
ください。わざと放っておいているわけではありません。

報告が確かめられたら、修正を次のリリースに入れて、アドバイザリも一緒に公開します。
クレジットは希望の名前で載せますし、載せたくなければ載せません。修正がプレイヤーに
行き渡るまでは、問題を公の場に書くのを待ってもらえると助かります。

## 対象になるもの

修正が入るのは最新のリリースで、古いバージョンへのバックポートはしません。なので
「1.0.x では直っていますか？」への答えは「アップデートしてください」になります。

| バージョン | サポート |
|---|---|
| 最新のリリース（[Releases](https://github.com/TomXV/dragnwash-modframework/releases)） | あり |
| それより古いもの | なし。まずアップデートしてください |

このリポジトリが配っているものは、全部が対象です。

- 中核プラグインとライブラリ（`src/`）、それとプリローダーのパッチャー
- ほかの Mod も使う共通インストーラー `Install.exe`（[`installer/`](installer/)）と、
  Steam Deck 用のスクリプト
- ゲームの前に動く更新用のランチャー `Launcher.exe`（[`launcher/`](launcher/)）
- クラッシュレポーター（[`crashreporter/`](crashreporter/)）と Code Graph
  （[`codegraph/`](codegraph/)、[`codegraph-standalone/`](codegraph-standalone/)）
- [`.github/workflows/`](.github/workflows/) のビルドとリリースのワークフロー

なかでもしっかり見てほしいのは、フレームワークが外の世界に触れるところです。

- **インストーラー**
  - ゲームのフォルダーを選んで、公式の BepInEx 5.4.23.5 をダウンロードし、ソースに
    書き込んである SHA-256 と合うか確かめてから展開します。Mod の `mod-install.json`
    も読みます。このファイルが ModFramework のリリースを指定していて（schema 2）、
    ゲームのフォルダーにそれが入っていなければ、バージョンだけから組み立てた URL で
    その zip を取ってきて、サイズと SHA-256 がこのファイルに書かれた値と合ったとき
    だけ使います。
  - 本物のバグと言えるのは、とどまるべきフォルダーの外に出てしまうパスや、別の
    アーカイブを受け入れさせたり、別の場所からダウンロードさせたりする方法です。
- **更新のお知らせ**
  - リポジトリを書いている Mod について、1 日 1 回まで `api.github.com` に最新の
    リリースを聞くだけです。プレイヤーのことは何も送らず、何もダウンロードしません。
    わかったことは、ランチャーのために `BepInEx/cache` に書いておきます。
  - これがゲームの中でのダウンロードに化けたり、プレイヤーの情報が漏れたりするなら、
    本物のバグです。
  - 詳しくは[外と通信する Mod の申告（wiki）](https://github.com/TomXV/dragnwash-modframework/wiki/Going-online-ja)にあります。
- **ランチャー**
  - インストーラーが設定した起動オプションで、Steam がゲームの前に起動します
    （インストーラーが書き換えるのは Steam の `localconfig.vdf` のその値 1 つだけです）。
    ゲームの更新チェックが書いたものを読み、プレイヤーが「更新」を押したときだけ、
    github.com と release-assets.githubusercontent.com にだけつなぎます。Mod の zip は
    リポジトリとタグから組み立てた URL で取ってきて、サイズと SHA-256 を GitHub の値と
    比べてから、インストーラーと同じコードで入れます。画面のページは exe に埋め込んで
    あり、外からは何も読み込みません。リリースノートは文字として表示します。
  - 別の場所からダウンロードさせる、合わないファイルを受け入れさせる、リリースノートの
    スクリプトを動かす、ゲームのフォルダーの外に書かせる、ほかの起動オプションや Steam
    の設定を変えさせる、といったことができるなら、本物のバグです。
- **アセットと Mod の読み込み**
  - `GameAssets` と Overrides ライブラリは、Mod のフォルダーからファイルを読みます。
  - 細工したファイルでそのフォルダーの外に手が届いたり、プレイヤーが入れていない
    コードを読み込ませたりできるなら、本物のバグです。
- **クラッシュレポーター**
  - レポートのファイルを書いて、それを表示します。
  - レポートには、必要なパスより多くプレイヤーのマシンの中身を入れない決まりです。

## 対象にならないもの

- **Mod はゲームにできることなら何でもできます。** Mod は BepInEx がゲームの
  プロセスに読み込む普通の .NET アセンブリです。フレームワークは使いやすい API を
  用意しているだけで、サンドボックスにはなっていませんし、Mod からできることを
  取り上げることもできません。「悪意のある Mod が自分のファイルを読めてしまう」のは
  どこの Mod でも同じで、このフレームワークの欠陥ではありません。信頼できる Mod
  だけを入れてください。
- **ゲーム本体。** Drag'n Wash 自体の不具合は開発元に伝えるもので、ここには
  当てはまりません。Mod を入れているときだけ起きるクラッシュも、開発元には送らないで
  ください。
- **BepInEx 本体。** [BepInEx のプロジェクト](https://github.com/BepInEx/BepInEx)に
  報告してください。
- **`Install.exe` がウイルス対策ソフトに誤検知される件。** 署名のないインストーラーは
  引っかかりやすいのですが、これは検知する側の癖で、脆弱性ではありません。どうすれば
  いいかは入れた Mod の README に書いてありますし、インストーラーのソースはここに
  あります。こういうものは普通の Issue で大丈夫です。
- **[Releases](https://github.com/TomXV/dragnwash-modframework/releases) 以外から
  手に入れたファイル。** リリースにはそれぞれ zip の SHA-256 を載せています。よそで
  手に入れたファイルが悪さをするなら、それはそのファイルの問題で、このプロジェクトとは
  関係ありません。それでも、どこで見つけたか教えてもらえると助かります。
